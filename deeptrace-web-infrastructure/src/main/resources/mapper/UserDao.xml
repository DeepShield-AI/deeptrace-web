<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.qcl.mapper.mysql.UserMapper">

    <resultMap type="cn.edu.qcl.user.User" id="UserMap">
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="role" column="role" jdbcType="VARCHAR"/>
        <result property="note" column="note" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="loginTime" column="login_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
user_id, username, password, phone, email, status, role, note, create_time, update_time, login_time
        from user
        where user_id = #{userId}
    </select>

    <!--查询单个-->
    <select id="queryByName" resultMap="UserMap">
        select
            user_id, username, password, phone, email, status, role, note, create_time, update_time, login_time
        from user
        where username = #{username}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
user_id, username, password, phone, email, status, role, note, create_time, update_time, login_time
        from user
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
            <if test="note != null and note != ''">
                and note = #{note}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="loginTime != null">
                and login_time = #{loginTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
            <if test="note != null and note != ''">
                and note = #{note}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="loginTime != null">
                and login_time = #{loginTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="userId" useGeneratedKeys="true">
        insert into user(username, password, phone, email, status, role, note, create_time, update_time, login_time)
        values (#{username}, #{password}, #{phone}, #{email}, #{status}, #{role}, #{note}, #{createTime}, #{updateTime}, #{loginTime})
    </insert>

    <insert id="insertBatch" keyProperty="userId" useGeneratedKeys="true">
        insert into user(username, password, phone, email, status, role, note, create_time, update_time, login_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.username}, #{entity.password}, #{entity.phone}, #{entity.email}, #{entity.status}, #{entity.role}, #{entity.note}, #{entity.createTime}, #{entity.updateTime}, #{entity.loginTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="userId" useGeneratedKeys="true">
        insert into user(username, password, phone, email, status, role, note, create_time, update_time, login_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.phone}, #{entity.email}, #{entity.status}, #{entity.role}, #{entity.note}, #{entity.createTime}, #{entity.updateTime}, #{entity.loginTime})
        </foreach>
        on duplicate key update
username = values(username),
password = values(password),
phone = values(phone),
email = values(email),
status = values(status),
role = values(role),
note = values(note),
create_time = values(create_time),
update_time = values(update_time),
login_time = values(login_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
            <if test="note != null and note != ''">
                note = #{note},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="loginTime != null">
                login_time = #{loginTime},
            </if>
        </set>
        where user_id = #{userId}
    </update>

    <!--更新用户登录时间-->
    <update id="updateLoginTime">
        UPDATE user
        SET login_time =NOW(),
            update_time = NOW()
        WHERE username = #{username}
    </update>


    <!--通过主键删除-->
    <delete id="deleteById">
        delete from user where user_id = #{userId}
    </delete>

</mapper>

